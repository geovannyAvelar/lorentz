# Explicitly specify the local powerDNS recursor as forward destination for .lorentz
# This ensures Lorentz knows that lorentz. is a local zone for which no DNSSEC
# validation should be done. Otherwise, we'd get BOGUS for everything as the
# root servers would tell us that lorentz. does not exist
server=/lorentz/127.0.0.1#5555

# Send the HTTPS/SVCB queries to the authoritative server without detour over the
# recursor because the latter returns SEVFAIL (connection between authoritative
# server and recursor is not encrypted)
server=/https.lorentz/127.0.0.1#5554
server=/svcb.lorentz/127.0.0.1#5554
# The zones below are served by the local authoritative PowerDNS instance but
# are *unsigned*. Mark them as local domains (same mechanism as .lorentz above) so
# dnsmasq performs no DNSSEC validation for them - otherwise validating the
# unsigned mask.icloud.com CNAME chain and the reverse zones issues internal
# DS/DNSKEY queries that walk up to the real ICANN root, whose key set drifts
# with rollovers and made the query counters flaky.
server=/icloud.com/127.0.0.1#5555
server=/apple-dns.net/127.0.0.1#5555
server=/in-addr.arpa/127.0.0.1#5555
server=/ip6.arpa/127.0.0.1#5555
